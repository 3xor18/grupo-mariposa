{
  "apiBaseUrl": "${API_BASE_URL}",
  "keycloakUrl": "${KEYCLOAK_URL}",
  "realm": "${KEYCLOAK_REALM}",
  "clientId": "${KEYCLOAK_CLIENT_ID}",
  "redirectUri": "${REDIRECT_URI}",
  "enableSemantics": ${ENABLE_SEMANTICS}
}
